import type { AppConfig } from '@vuepress/core';
import type { BuildCommandOptions } from './types';
export declare const resolveBuildAppConfig: (sourceDir: string | undefined, { dest, temp, cache, debug }: BuildCommandOptions, cwd?: string) => AppConfig;
