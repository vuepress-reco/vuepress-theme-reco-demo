"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.renderPageStyles = void 0;
/**
 * Render styles of current page
 */
const renderPageStyles = ({ app, initialFilesMeta, pageClientFilesMeta, }) => 
// include initial CSS files and other async CSS files of current page
// notice here we put async CSS files after initial CSS files
[...initialFilesMeta, ...pageClientFilesMeta]
    .filter(({ type }) => type === 'style')
    .map(({ file }) => `<link rel="stylesheet" href="${app.options.base}${file}">`)
    .join('');
exports.renderPageStyles = renderPageStyles;
