"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.renderPageScripts = void 0;
/**
 * Render scripts of current page
 */
const renderPageScripts = ({ app, initialFilesMeta, pageClientFilesMeta, }) => 
// include initial JS files and other async JS files of current page
[...pageClientFilesMeta, ...initialFilesMeta]
    .filter(({ type }) => type === 'script')
    .map(({ file }) => `<script src="${app.options.base}${file}" defer></script>`)
    .join('');
exports.renderPageScripts = renderPageScripts;
