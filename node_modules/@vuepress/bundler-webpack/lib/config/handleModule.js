"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.handleModule = void 0;
const handleModuleAssets_1 = require("./handleModuleAssets");
const handleModuleJs_1 = require("./handleModuleJs");
const handleModulePug_1 = require("./handleModulePug");
const handleModuleStyles_1 = require("./handleModuleStyles");
const handleModuleTs_1 = require("./handleModuleTs");
const handleModuleVue_1 = require("./handleModuleVue");
/**
 * Set webpack module
 */
const handleModule = ({ app, options, config, isServer, isBuild, }) => {
    // noParse
    config.module.noParse(/(^(vue|vue-router|vuex|vuex-router-sync)$)|(^@vue\/[^/]*$)/);
    // vue files
    handleModuleVue_1.handleModuleVue({ app, options, config, isServer });
    // pug files, for templates
    handleModulePug_1.handleModulePug({ config });
    // images & media & fonts
    handleModuleAssets_1.handleModuleAssets({ app, config });
    // js files
    handleModuleJs_1.handleModuleJs({ options, config, isServer, isBuild });
    // ts files
    handleModuleTs_1.handleModuleTs({ app, config });
    // styles files
    handleModuleStyles_1.handleModuleStyles({ app, options, config, isServer, isBuild });
};
exports.handleModule = handleModule;
