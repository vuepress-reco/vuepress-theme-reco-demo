"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolvePluginsFromConfig = void 0;
const normalizePluginConfig_1 = require("./normalizePluginConfig");
const resolvePlugin_1 = require("./resolvePlugin");
/**
 * Resolve plugins from plugin config array
 */
const resolvePluginsFromConfig = (app, plugins = []) => plugins.reduce((prev, item) => {
    const [plugin, config] = normalizePluginConfig_1.normalizePluginConfig(item);
    if (config !== false) {
        prev.push(resolvePlugin_1.resolvePlugin(app, plugin, config === true ? {} : config));
    }
    return prev;
}, []);
exports.resolvePluginsFromConfig = resolvePluginsFromConfig;
