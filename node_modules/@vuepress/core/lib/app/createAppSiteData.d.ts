import type { AppOptions, SiteData } from '../types';
/**
 * Create site data for vuepress app
 *
 * Site data will also be used in client
 */
export declare const createAppSiteData: (options: AppOptions) => SiteData;
