import type { App, PageOptions } from '../types';
/**
 * Render page content and extract related info
 */
export declare const resolvePageOptions: ({ app, optionsRaw, }: {
    app: App;
    optionsRaw: PageOptions;
}) => Promise<PageOptions>;
