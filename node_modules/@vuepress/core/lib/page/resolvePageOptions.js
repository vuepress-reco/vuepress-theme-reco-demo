"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolvePageOptions = void 0;
/**
 * Render page content and extract related info
 */
const resolvePageOptions = async ({ app, optionsRaw, }) => {
    // avoid mutating the original options object provided by user
    const options = { ...optionsRaw };
    // plugin hook: extendsPageOptions
    const extendsPageOptions = await app.pluginApi.hooks.extendsPageOptions.process(options, app);
    extendsPageOptions.forEach((item) => Object.assign(options, item));
    return options;
};
exports.resolvePageOptions = resolvePageOptions;
