import type { App } from 'vue';
import type { Router } from 'vue-router';
import type { PageData, PageFrontmatter, PageHeadTitle, PageLang, RouteLocale, SiteData, SiteLocaleData } from './composables';
import { withBase } from './utils';
/**
 * Create and provide global computed
 */
export declare const provideGlobalComputed: (app: App, router: Router) => void;
declare module '@vue/runtime-core' {
    interface ComponentCustomProperties {
        $frontmatter: PageFrontmatter;
        $headTitle: PageHeadTitle;
        $lang: PageLang;
        $page: PageData;
        $routeLocale: RouteLocale;
        $site: SiteData;
        $siteLocale: SiteLocaleData;
        $withBase: typeof withBase;
    }
}
